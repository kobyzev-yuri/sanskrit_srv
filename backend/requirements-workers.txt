# Optional extras (OCR / WeasyPrint / Celery) — not required for alpha runtime.
# Alpha uses: backend/requirements.txt + python -m app.worker (PyMuPDF + ProxyAPI).

# --- PDF classify / native extract ---
pdf-inspector>=0.2.6

# --- raster + images ---
pymupdf>=1.24
pillow>=10.0

# --- OCR (needs system tesseract-ocr + tesseract-ocr-san/eng) ---
pytesseract>=0.3

# --- HTML normalize ---
beautifulsoup4>=4.12
lxml>=5.0

# --- export ---
weasyprint>=62

# --- LLM draft (ProxyAPI / OpenAI-compatible) ---
httpx>=0.27

# --- Indic scripts (IAST, optional post-process) ---
indic-transliteration>=2.3

# --- jobs (when wiring Celery workers) ---
celery[redis]>=5.4
redis>=5.0
pydantic>=2.0
pydantic-settings>=2.0

# Worker toolkit for Sanskrit scan service
# Shared processing stack: classify → OCR/extract → LLM draft → export
# See docs/toolkit.md

# --- PDF classify / native extract ---
pdf-inspector>=0.2.6

# --- raster + images ---
pymupdf>=1.24
pillow>=10.0

# --- OCR (needs system tesseract-ocr + tesseract-ocr-san/eng) ---
pytesseract>=0.3

# --- HTML normalize ---
beautifulsoup4>=4.12
lxml>=5.0

# --- export ---
weasyprint>=62

# --- LLM draft (ProxyAPI / OpenAI-compatible) ---
httpx>=0.27

# --- Indic scripts (IAST, optional post-process) ---
indic-transliteration>=2.3

# --- jobs (when wiring Celery workers) ---
celery[redis]>=5.4
redis>=5.0
pydantic>=2.0
pydantic-settings>=2.0
